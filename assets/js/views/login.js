// Layar login dan pembuatan owner pertama.
import { api, app, esc, hideLoading, icon, notify, showLoading } from '../core.js';
import { state } from '../state.js';
import { ke } from '../router.js';

export async function renderLogin(error = '') { app.innerHTML = `<main class="login-page"><section class="login-card"><div class="brand"><img src="/logo.jpg" alt="" class="brand-logo" width="26" height="26">Gutok<span>Drive</span></div><h1>Simpan hal penting,<br>tetap terasa ringan.</h1><p class="subtle">Masuk ke ruang penyimpanan pribadimu.</p><form id="login-form"><div class="field"><label>Email atau username</label><input name="identity" required autocomplete="username"></div><div class="field"><label>Password</label><input name="password" type="password" required autocomplete="current-password"></div>${error ? `<div class="error">${esc(error)}</div>` : ''}<button class="primary btn-block mt-md">Masuk ${icon('arrow-right')}</button></form><div id="setup-link" class="setup-link"></div></section></main>`; document.querySelector('#login-form').onsubmit = async (event) => { event.preventDefault(); try { const data = await api('/api/login', { method:'POST', body:JSON.stringify(Object.fromEntries(new FormData(event.target))) }); state.user = data.user; await ke('dashboard'); } catch (error) { ke('login', error.message); } finally { hideLoading(); } }; try { const setup = await api('/api/setup'); if (setup.needsSetup) document.querySelector('#setup-link').innerHTML = `<button class="side-link mx-auto" id="setup-owner">Setup owner pertama</button>`; document.querySelector('#setup-owner')?.addEventListener('click', renderSetup); } catch {} }
export function renderSetup() { app.innerHTML = `<main class="login-page"><section class="login-card"><div class="brand"><img src="/logo.jpg" alt="" class="brand-logo" width="26" height="26">Gutok<span>Drive</span></div><h1>Buat owner pertama</h1><p class="subtle">Akun ini akan memiliki akses penuh ke dashboard.</p><form id="setup-form"><div class="field"><label>Email</label><input name="email" type="email" required></div><div class="field"><label>Username</label><input name="username" required></div><div class="field"><label>Password, minimal 8 karakter</label><input name="password" type="password" minlength="8" required></div><button class="primary btn-block mt-md">Buat akun ${icon('arrow-right')}</button></form></section></main>`; document.querySelector('#setup-form').onsubmit = async (event) => { event.preventDefault(); try { await api('/api/setup', { method:'POST', body:JSON.stringify(Object.fromEntries(new FormData(event.target))) }); ke('login'); notify('Owner berhasil dibuat.'); } catch (error) { notify(error.message); } finally { hideLoading(); } }; }
