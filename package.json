{
  "name": "gutok-drive",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "cek-sintaks": "node --check server.js && node --check cleanup.js && for f in assets/app.js assets/js/*.js assets/js/views/*.js; do node --check \"$f\" || exit 1; done",
    "uji": "node uji/lingkup-app.mjs && node uji/upload.mjs && node uji/layar.mjs && node uji/gaya.mjs",
    "uji:berat": "node uji/cleanup.mjs && node uji/server-lambat.mjs && node uji/ram.mjs",
    "check": "npm run cek-sintaks && npm run uji"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "better-sqlite3": "^11.10.0",
    "express": "^5.1.0",
    "megajs": "^1.3.10",
    "multer": "^2.0.2"
  }
}
